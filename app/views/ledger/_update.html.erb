<%= render 'shared/container', class:'ct-ledger-update', tabs: [
    {id: 'tabDel', active:true, view:'shared/delete', label: 'Delete',
    locals: {url: ledger_destroy_path(id: @entry.id)}},
    {label: 'Cancel', path: ledger_url}
] %>
