<%
  one = find_vm_by_kind(:trade)
  two = find_vm_by_kind(:trade_ticker) %>
<div id="ct-dashboard">
  <div class="row justify-content-end">
    <div class="col-1 text-right mb-2">
      <%= link_to 'Back', trade_url, class: 'btn btn-sm btn-outline-secondary' %>
    </div>
  </div>
  <%= render 'shared/two_cols', one: one, two: two %>
</div>