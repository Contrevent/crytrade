<% content = local_assigns.fetch(:content)
kind = local_assigns.key?(:kind) ? local_assigns.fetch(:kind) : nil
%>
<div class="ct-table-cell-chip<%= kind != nil ? ' alert-' + kind: ''%>">
  <%= content %>
</div>